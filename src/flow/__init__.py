"""Flow - A vibe coding CLI tool with AI-powered assistance."""

import logging

__version__ = "0.1.0"

# Configure logging for the flow package
logging.getLogger(__name__).addHandler(logging.NullHandler())
